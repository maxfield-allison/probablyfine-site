---
// Loads the copy-button script. The post page renders this only when the page
// has a `$` command block, so every other page ships no copy JavaScript.
---

<script src="../scripts/copy-commands.js"></script>
